import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    width: '100%',
    flexDirection: 'row',
    backgroundColor: '#222831'
  },

  route: {
    flex: 1,
    borderColor: '#000',
    borderWidth: 1
  },

  routeText: {
    textAlign: 'center',
    color: '#EEEEEE'
  }
})
